import { StyleSheet } from "react-native";

export default StyleSheet.create({
  commonText: ({
    color,
    fontSize,
    fontWeight,
    fontFamily,
    lineHeight,
    maxWidth,
  }) => ({
    color,
    fontSize,
    fontWeight,
    fontFamily,
    lineHeight,
    maxWidth,
  }),
});
