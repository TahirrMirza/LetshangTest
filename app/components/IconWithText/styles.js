import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    gap: 20,
  },
  secContainer: {
    gap: 6,
  },
  leftIconStyles: {
    alignSelf: "flex-start",
  },
});
